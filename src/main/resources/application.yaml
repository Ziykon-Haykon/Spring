spring:
  application:
    name: Spring

  datasource:
    url: jdbc:postgresql://localhost:5432/testdb
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true